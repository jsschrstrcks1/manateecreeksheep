# Where is Sophos from here?
<!-- generated by admin/write-household-root-file.mjs — edit the generator, not the copies -->

You are in a household repo. The governing posture (Sophos, Soli Deo Gloria,
careful-not-clever) is NOT stored here — it is resolved. This file tells you how.

If you cannot resolve a root below, SAY SO PLAINLY. An agent that cannot reach
Layer 0 is UNGOVERNED, and reporting that is the compliant outcome — proceeding
quietly is not. Fail loud is household posture for everything.

## doctrine — Project-Sophos
the Sophos OS contract and design authority.

Resolve in this order, and NAME which candidate won:
  1. $HOUSEHOLD_SOPHOS_ROOT (or $HOUSEHOLD_DOCTRINE_ROOT)
  2. a sibling checkout: ../Project-Sophos
  3. walk up from here, looking for Project-Sophos

Confirm it is really the root — one of these must exist inside it:
  - ARCHITECTURE.md
  - ROADMAP.md

## runtime — open-claw-stuff
the running kernel, guards and HLS SSOT.

Resolve in this order, and NAME which candidate won:
  1. $HOUSEHOLD_OCS_ROOT (or $HOUSEHOLD_RUNTIME_ROOT)
  2. a sibling checkout: ../open-claw-stuff
  3. walk up from here, looking for open-claw-stuff

Confirm it is really the root — one of these must exist inside it:
  - .household-library
  - atlas/server/sophos.mjs

## Do it mechanically

```
node <runtime-root>/admin/household-root.mjs            # report both roots
node <runtime-root>/admin/household-root.mjs --check     # exit 2 if either is unresolved
```

No absolute path appears in this file on purpose. One machine's layout committed
here would be dead on every other machine, and would look authoritative while it was.

Soli Deo Gloria.
